import React from "react";
import { ScrollView, View, Text } from "react-native";

export const ApplicantStatus = () => {

    return (
        <ScrollView>
            <View>
                <Text>지원자 현황</Text>
            </View>
        </ScrollView>
    )
}